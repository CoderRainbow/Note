package com.gate;

/**
 * synchronized:
 *   synchronized锁升级：无锁 ——> 偏向锁 ——> 轻量级锁 ——> 重量级锁。
 * volatile:
 *   volatile可见性和禁止指令排序是怎么实现的。
 *   可见性：缓存一致性协议。
 *   禁止指令重排序：添加内存屏障。
 * AtomicXXX:
 * <p>
 * 各种JUC同步锁：
 *   ReentrantLock
 *   CountDownLatch
 *   CyclicBarrier
 *   Phaser
 *   ReadWriteLock
 *   StampedLock
 *   Semaphore
 *   Exchanger
 *   LockSupport
 */
public class readme {
}
